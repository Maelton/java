public class OperadorTernario {
    
    public static void main(String[] args) {

        /**
         * Utiliza-se os simbolos de interrogação e dois pontos para criar operações ternárias em Java da seguinte maneira:
        
            condicao ? (valor se verdadeiro) : (valor se falso)
         */

         float altura = 1.88f;
         boolean ehAlto = altura > 1.8 ? true : false;
    
    }   
}
